# Plethora

## Engine

1. setup .env
2. run `node utils/generateWallets.json`
3. run `node utils/fund_wallets.json`
4. setup the x402 server
5. run `node test-single-agent.js`

## x402 test server

1. setup .env
2. run `node server.js`